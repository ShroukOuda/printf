#ifndef MAIN_H
#define MAIN_H
#include <limits.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
int _putchar(char c);
int _printf(const char *format, ...);
void number(int i);
void string(char *str);
void va_start(va_list ap, INT_MAX);

#endif /* MAIN_H */
